model CreditExchangeRate {
  id            String   @id @default(uuid()) @db.Uuid
  creditsPerThb Decimal  @db.Decimal(20, 6)
  effectiveFrom DateTime @default(now())
  effectiveTo   DateTime?

  topups CreditTopupHistory[]

  @@map("credit_exchange_rates")
}